[variables]
main_domain = "${domain}"
huly_secret = "${base64:64}"
cockroach_password = "${password:32}"
redpanda_password = "${password:16}"
livekit_api_key = "${password:16}"
livekit_api_secret = "${base64:32}"

[config]
env = [
  "HULY_VERSION=v0.7.353",
  "TEMPLATE_VERSION=v3.0.9",
  "DOCKER_NAME=huly_v7",
  "HOST_ADDRESS=${main_domain}",
  "SECRET=${huly_secret}",
  "TITLE=Huly",
  "DEFAULT_LANGUAGE=en",
  "LAST_NAME_FIRST=true",
  "CR_DATABASE=huly",
  "CR_USERNAME=huly",
  "CR_PASSWORD=${cockroach_password}",
  "CR_DB_URL=postgres://huly:${cockroach_password}@cockroach:26257/huly",
  "STORAGE_CONFIG=datalake|http://datalake:4031",
  "REDPANDA_ADMIN_USER=admin",
  "REDPANDA_ADMIN_PWD=${redpanda_password}",
  "PLATFORM_ADMIN_EMAILS=",
  "# LiveKit Configuration (built-in - auto-generated keys)",
  "LIVEKIT_API_KEY=${livekit_api_key}",
  "LIVEKIT_API_SECRET=${livekit_api_secret}",
  "# GitHub Integration (optional - requires GitHub App)",
  "GITHUB_APPID=",
  "GITHUB_APPNAME=",
  "GITHUB_CLIENTID=",
  "GITHUB_CLIENT_SECRET=",
  "GITHUB_PRIVATE_KEY=",
  "GITHUB_BOT_NAME=",
  "# Email / SMTP Configuration (required for OTP login)",
  "MAIL_FROM=",
  "SMTP_HOST=",
  "SMTP_PORT=587",
  "SMTP_USERNAME=",
  "SMTP_PASSWORD=",
  "# Amazon SES (alternative to SMTP - leave blank if using SMTP)",
  "SES_ACCESS_KEY=",
  "SES_SECRET_KEY=",
  "SES_REGION=",
  "# AI Assistant Configuration (optional - requires OPENAI_API_KEY for AI features)",
  "OPENAI_API_KEY=",
  "OPENAI_BASE_URL=",
  "OPENAI_MODEL=",
  "OPENAI_SUMMARY_MODEL=",
  "OPENAI_TRANSLATE_MODEL=",
  "AI_BOT_FIRST_NAME=Huly",
  "AI_BOT_LAST_NAME=AI",
  "# Google Calendar & Gmail Integration (optional - requires Google OAuth credentials)",
  "GOOGLE_CREDENTIALS=",
  "# Telegram Bot Integration (optional - requires bot token from @BotFather)",
  "TELEGRAM_BOT_TOKEN=",
  "# Speech-to-Text for Meeting Transcription (optional - used by love-agent)",
  "# STT_PROVIDER: 'deepgram' (default) or 'openai' (uses Realtime API, not Whisper)",
  "STT_PROVIDER=deepgram",
  "# For Deepgram: set DEEPGRAM_API_KEY (default model: nova-3)",
  "DEEPGRAM_API_KEY=",
  "# For OpenAI: uses OPENAI_API_KEY from above (default model: gpt-4o-transcribe)",
  "LOVE_AGENT_DEBUG=false",
  "# Meeting-minutes counter reconciliation interval in seconds (workaround for upstream bug)",
  "RECONCILE_INTERVAL=300",
]

[[config.domains]]
serviceName = "nginx"
port = 80
host = "${main_domain}"

# Entrypoint script that calculates parent domain and patches nginx config
[[config.mounts]]
filePath = "/volumes/nginx/entrypoint.sh"
content = """
#!/bin/sh
set -e

# Calculate parent domain from HOST_ADDRESS
# huly.example.com -> example.com
# app.huly.example.com -> huly.example.com
PARENT_DOMAIN=$(echo "$HOST_ADDRESS" | sed 's/^[^.]*\.//')

echo "=== Huly V7 Nginx Entrypoint ==="
echo "HOST_ADDRESS: $HOST_ADDRESS"
echo "PARENT_DOMAIN: $PARENT_DOMAIN"

# Always regenerate config from template (template is read-only, never modified)
cp /etc/nginx/nginx.template /etc/nginx/conf.d/default.conf
sed -i "s/PARENT_DOMAIN_PLACEHOLDER/.$PARENT_DOMAIN/g" /etc/nginx/conf.d/default.conf
sed -i "s/HOST_ADDRESS_PLACEHOLDER/$HOST_ADDRESS/g" /etc/nginx/conf.d/default.conf

echo "Nginx config generated from template!"
echo "Starting nginx..."

# Start nginx in foreground
exec nginx -g 'daemon off;'
"""

# Nginx config with all V7 routes and cookie fixes
# All upstreams use variables for lazy DNS resolution — nginx starts even if backends aren't ready
[[config.mounts]]
filePath = "/volumes/nginx/.huly.nginx"
content = """
server {
    listen 80;
    server_name _;
    client_max_body_size 0;

    # Docker embedded DNS - required for variable-based upstream resolution
    resolver 127.0.0.11 valid=10s;

    # Frontend
    location / {
        set $upstream_front http://front:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_pass $upstream_front;
    }

    # Accounts - WITH COOKIE FIXES FOR V7 SESSION PERSISTENCE
    location /_accounts {
        set $upstream_account http://account:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # IMPORTANT: Hardcode https - $scheme returns "http" inside Docker container
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_accounts$ / break;
        rewrite ^/_accounts(/.*)$ $1 break;

        # Cookie handling fixes for V7 session persistence
        proxy_cookie_path / /;
        proxy_cookie_flags ~ secure samesite=lax;
        # Dynamic cookie domain - replaced by entrypoint.sh
        proxy_cookie_domain PARENT_DOMAIN_PLACEHOLDER HOST_ADDRESS_PLACEHOLDER;

        proxy_pass $upstream_account;
    }

    # Love (Video calls service)
    location /_love {
        set $upstream_love http://love:8097;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        rewrite ^/_love(/.*)$ $1 break;
        proxy_pass $upstream_love;
    }

    # Collaborator (WebSocket)
    location /_collaborator {
        set $upstream_collaborator http://collaborator:3078;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        rewrite ^/_collaborator(/.*)$ $1 break;
        proxy_pass $upstream_collaborator;
    }

    # Transactor (WebSocket)
    location /_transactor {
        set $upstream_transactor http://transactor:3333;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        rewrite ^/_transactor(/.*)$ $1 break;
        proxy_pass $upstream_transactor;
    }

    # Transactor token shortcuts (JWT paths)
    location ~ ^/eyJ {
        set $upstream_transactor_jwt http://transactor:3333;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_pass $upstream_transactor_jwt;
    }

    # Rekoni
    location /_rekoni {
        set $upstream_rekoni http://rekoni:4004;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_rekoni(/.*)$ $1 break;
        proxy_pass $upstream_rekoni;
    }

    # Stats
    location /_stats {
        set $upstream_stats http://stats:4900;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_stats(/.*)$ $1 break;
        proxy_pass $upstream_stats;
    }

    # Pulse (WebSocket)
    location /_pulse {
        set $upstream_pulse http://hulypulse:8098;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        rewrite ^/_pulse(.*)$ /ws$1 break;
        proxy_pass $upstream_pulse;
    }

    # Stream
    location /_stream {
        set $upstream_stream http://stream:1081;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_stream(.*)$ $1 break;
        proxy_pass $upstream_stream;
    }

    # Preview
    location /_preview {
        set $upstream_preview http://preview:4043;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_preview(.*)$ $1 break;
        proxy_pass $upstream_preview;
    }

    # Datalake
    location /_datalake {
        set $upstream_datalake http://datalake:4031;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        rewrite ^/_datalake(/.*)$ $1 break;
        proxy_pass $upstream_datalake;
    }

    # GitHub Integration
    location /_github {
        set $upstream_github http://github:3500;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        rewrite ^/_github(/.*)$ $1 break;
        proxy_pass $upstream_github;
    }

    # Print
    location /_print {
        set $upstream_print http://print:4005;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        rewrite ^/_print(/.*)$ $1 break;
        proxy_pass $upstream_print;
    }

    # AI Bot
    location /_ai {
        set $upstream_ai http://aibot:4010;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        rewrite ^/_ai(/.*)$ $1 break;
        proxy_pass $upstream_ai;
    }

    # Calendar Integration
    location /_calendar {
        set $upstream_calendar http://calendar:8095;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        rewrite ^/_calendar(/.*)$ $1 break;
        proxy_pass $upstream_calendar;
    }

    # Gmail Integration
    location /_gmail {
        set $upstream_gmail http://gmail:8087;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        rewrite ^/_gmail(/.*)$ $1 break;
        proxy_pass $upstream_gmail;
    }

    # Telegram Bot Integration
    location /_telegram {
        set $upstream_telegram http://telegram-bot:4020;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        rewrite ^/_telegram(/.*)$ $1 break;
        proxy_pass $upstream_telegram;
    }

    # LiveKit signaling (WebSocket)
    location /livekit/ {
        set $upstream_livekit http://livekit:7880;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;
        proxy_send_timeout 86400;
        proxy_buffering off;
        rewrite ^/livekit(/.*)$ $1 break;
        proxy_pass $upstream_livekit;
    }
}
"""

# LiveKit entrypoint script (replaces placeholders with env vars at runtime)
[[config.mounts]]
filePath = "/volumes/livekit/entrypoint.sh"
content = """
#!/bin/sh
set -e

echo "=== LiveKit Entrypoint ==="
echo "Injecting API keys and domain into config..."

cp /etc/livekit.template /etc/livekit.yaml
sed -i "s|LIVEKIT_KEY_PLACEHOLDER|$LIVEKIT_API_KEY|g" /etc/livekit.yaml
sed -i "s|LIVEKIT_SECRET_PLACEHOLDER|$LIVEKIT_API_SECRET|g" /etc/livekit.yaml
sed -i "s|LIVEKIT_DOMAIN_PLACEHOLDER|$HOST_ADDRESS|g" /etc/livekit.yaml

echo "LiveKit config ready. Starting server..."
exec /livekit-server --config /etc/livekit.yaml
"""

# LiveKit server configuration template (placeholders replaced by entrypoint.sh)
[[config.mounts]]
filePath = "/volumes/livekit/livekit.yaml"
content = """
port: 7880
bind_addresses:
  - 0.0.0.0
rtc:
  tcp_port: 7881
  port_range_start: 50000
  port_range_end: 50100
  use_external_ip: true
  enable_loopback_candidate: false
redis:
  address: redis:6379
turn:
  enabled: true
  domain: LIVEKIT_DOMAIN_PLACEHOLDER
  udp_port: 3478
  external_tls: true
keys:
  LIVEKIT_KEY_PLACEHOLDER: LIVEKIT_SECRET_PLACEHOLDER
logging:
  json: false
  level: info
"""

# Love Agent entrypoint script (generates PLATFORM_TOKEN at runtime)
# The hardcoreeng love-agent requires a PLATFORM_TOKEN JWT containing the
# aibot's personUuid, signed with SERVER_SECRET. This script discovers the
# personUuid by logging in as the AI bot via the accounts service.
# Fallback: generates a system JWT (HS256) to query the ai-bot's PersonUUID,
# then mints PLATFORM_TOKEN directly.
[[config.mounts]]
filePath = "/volumes/love-agent/entrypoint.sh"
content = """
#!/bin/sh
set -e

echo "=== Love Agent Entrypoint ==="
echo "Generating PLATFORM_TOKEN for love-agent..."

cat > /tmp/gen-token.js << 'GENTOKEN'
const crypto = require('crypto');
const http = require('http');

const SERVER_SECRET = process.env.SERVER_SECRET;
const SYSTEM_UUID = '1749089e-22e6-48de-af4e-165e18fbd2f9';
const BOT_EMAIL = 'huly.ai.bot@hc.engineering';

// --- HS256 JWT (same as Huly's jwt-simple) ---
function b64url(buf) {
  return (Buffer.isBuffer(buf) ? buf : Buffer.from(buf))
    .toString('base64').replace(/\\+/g, '-').replace(/\\//g, '_').replace(/=/g, '');
}
function signJWT(payload, secret) {
  const h = b64url(JSON.stringify({ typ: 'JWT', alg: 'HS256' }));
  const p = b64url(JSON.stringify(payload));
  const sig = b64url(crypto.createHmac('sha256', secret).update(h + '.' + p).digest());
  return h + '.' + p + '.' + sig;
}

// --- HTTP helpers ---
function post(host, port, body, token) {
  return new Promise((resolve, reject) => {
    const data = JSON.stringify(body);
    const headers = {
      'Content-Type': 'application/json',
      'Content-Length': Buffer.byteLength(data)
    };
    if (token) headers['Authorization'] = 'Bearer ' + token;
    const req = http.request({ hostname: host, port, method: 'POST', path: '/', headers }, (res) => {
      let chunks = '';
      res.on('data', c => chunks += c);
      res.on('end', () => {
        try { resolve(JSON.parse(chunks)); }
        catch (e) { reject(new Error('Bad JSON: ' + chunks.slice(0, 200))); }
      });
    });
    req.on('error', reject);
    req.write(data);
    req.end();
  });
}
function sleep(ms) { return new Promise(r => setTimeout(r, ms)); }

async function getToken() {
  // --- Method 1: login RPC (object params, v0.7.353 format) ---
  const r1 = await post('account', 3000, {
    method: 'login',
    params: { email: BOT_EMAIL, password: 'password' }
  });
  if (r1.result) {
    const t = typeof r1.result === 'string' ? r1.result : r1.result.token;
    if (t) { process.stderr.write('Method 1 (login) succeeded\\n'); return t; }
  }
  process.stderr.write('Method 1 (login) failed: ' + JSON.stringify(r1).slice(0, 300) + '\\n');

  // --- Method 2: generate system JWT, find ai-bot personUuid, mint token ---
  if (!SERVER_SECRET) throw new Error('SERVER_SECRET not set, cannot generate JWT');
  const sysToken = signJWT({ account: SYSTEM_UUID, extra: { service: 'love-agent' } }, SERVER_SECRET);

  // Try findPersonBySocialKey with different param names
  for (const paramName of ['key', 'socialKey']) {
    const r2 = await post('account', 3000, {
      method: 'findPersonBySocialKey',
      params: { [paramName]: 'email:' + BOT_EMAIL }
    }, sysToken);
    if (r2.result) {
      const uuid = typeof r2.result === 'string' ? r2.result : r2.result.uuid || r2.result;
      if (uuid && typeof uuid === 'string') {
        process.stderr.write('Method 2 (findPersonBySocialKey/' + paramName + ') found: ' + uuid + '\\n');
        return signJWT({ account: uuid, extra: { service: 'love-agent' } }, SERVER_SECRET);
      }
    }
    process.stderr.write('findPersonBySocialKey(' + paramName + '): ' + JSON.stringify(r2).slice(0, 300) + '\\n');
  }

  // Try getPerson as another fallback
  const r3 = await post('account', 3000, {
    method: 'getPerson',
    params: { email: BOT_EMAIL }
  }, sysToken);
  if (r3.result) {
    const uuid = typeof r3.result === 'string' ? r3.result :
      (r3.result.uuid || r3.result.personUuid || r3.result._id);
    if (uuid && typeof uuid === 'string') {
      process.stderr.write('Method 2 (getPerson) found: ' + uuid + '\\n');
      return signJWT({ account: uuid, extra: { service: 'love-agent' } }, SERVER_SECRET);
    }
  }
  process.stderr.write('getPerson: ' + JSON.stringify(r3).slice(0, 300) + '\\n');

  throw new Error('All methods failed');
}

async function main() {
  for (let attempt = 1; attempt <= 60; attempt++) {
    try {
      const token = await getToken();
      process.stdout.write(token);
      return;
    } catch (e) {
      process.stderr.write('Attempt ' + attempt + '/60: ' + e.message + '\\n');
    }
    await sleep(5000);
  }
  process.stderr.write('FATAL: Could not generate PLATFORM_TOKEN after 60 attempts\\n');
  process.exit(1);
}

main();
GENTOKEN

PLATFORM_TOKEN=$(node /tmp/gen-token.js)
export PLATFORM_TOKEN
rm -f /tmp/gen-token.js

if [ -z "$PLATFORM_TOKEN" ]; then
  echo "ERROR: PLATFORM_TOKEN is empty"
  exit 1
fi

echo "PLATFORM_TOKEN generated successfully (${#PLATFORM_TOKEN} chars)"
echo "Starting love-agent..."
exec node /usr/src/app/index.js start
"""

# CockroachDB reconciliation job: fixes meeting-minutes counter bug
# (upstream PR #10527 — MeetingMinutes created via createDoc instead of addCollection,
# so Room.meetings counter is never incremented and UI shows "No meeting minutes")
# Remove this once the upstream fix is released.
[[config.mounts]]
filePath = "/volumes/cockroach-jobs/reconcile.sh"
content = """
#!/bin/sh
set -e

INTERVAL=${RECONCILE_INTERVAL:-300}

echo "Waiting for CockroachDB to be ready..."
until psql "$CR_DB_URL" -c "SELECT 1" > /dev/null 2>&1; do
  sleep 5
done
echo "CockroachDB is ready. Running reconciliation every ${INTERVAL}s."

while true; do
  psql "$CR_DB_URL" -c "
    UPDATE love
    SET data = jsonb_set(COALESCE(data, '{}'::jsonb), '{meetings}', (
        SELECT COALESCE(to_jsonb(count(*)), '0'::jsonb)
        FROM meeting_minutes
        WHERE \"attachedTo\" = love._id
        AND \"workspaceId\" = love.\"workspaceId\"
    )::jsonb)
    WHERE _class IN ('love:class:Room', 'love:class:Office');
  " && echo "[$(date -Iseconds)] Reconciled meeting-minutes counters" \
    || echo "[$(date -Iseconds)] Reconciliation failed"
  sleep "$INTERVAL"
done
"""
